{
  "routes": [
    {
      "method": "GET",
      "path": "/workflows",
      "handler": "workflow.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/workflows/count",
      "handler": "workflow.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/workflows/:id",
      "handler": "workflow.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/workflows",
      "handler": "workflow.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/workflows/:id",
      "handler": "workflow.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/workflows/:id",
      "handler": "workflow.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
