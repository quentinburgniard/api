{
  "routes": [
    {
      "method": "GET",
      "path": "/identities",
      "handler": "identity.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/identities/count",
      "handler": "identity.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/identities/:id",
      "handler": "identity.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/identities",
      "handler": "identity.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/identities/:id",
      "handler": "identity.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/identities/:id",
      "handler": "identity.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
